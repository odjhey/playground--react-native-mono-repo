{
  "name": "core",
  "version": "0.0.1",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "compile": "swc src --out-dir dist",
    "compile-types": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.5",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "mobx": "^6.6.2",
    "mobx-state-tree": "^5.1.6"
  }
}
